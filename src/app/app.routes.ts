import { Routes } from '@angular/router';
import { StartPageComponent } from './features/start_page/start-page.component';
export const routes: Routes = [
    { path: '', component: StartPageComponent },
];
